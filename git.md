create new branch with uncommited files: git checkout -b branch-name // this will not harm current branch with uncommited files. rather it takes uncommited files to new branch and keep current safe. 
set this branch with git: git push -u origin branch-name
switch between branches: git switch branch-name
